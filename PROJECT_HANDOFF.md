# Передача контекста проекта

## Текущее состояние

Рабочая ветка — `refactor/decompose-large-modules`; рабочее дерево содержит
локальные незакоммиченные изменения декомпозиции. Коммит и отправка в удалённый
репозиторий не выполнялись. Для фото-парсера закреплён снимок
vision-модели `gpt-5.4-mini-2026-03-17`; при `reasoning.effort=none` запрос
использует `temperature=0` для уменьшения вариативности.

22.09 исправлены подтверждённые ошибки фото и выбора подразделений. Экспорт
`vmui_logs_export.jsonl` показывает успешные ответы Vision с последующим отказом
обработчика: расхождение пиксельного счётчика и ответа, несовпадение двух чтений,
а также ноль допущенных позиций при заполненных именованных полях отделов и N/O/P.
Контейнер в этом экспорте создан ночью: версия о пересборке непосредственно за
пять минут до отказа не подтверждена. Изменения ограничены этими дефектами и
регрессиями по ADR-019; модель, промпты и инфраструктура не изменялись.

Названия «Зал/Бар/Кухня» и буквы N/O/P — альтернативные доказательства отдела:
N — Зал, O — Бар, P — Кухня. Для N/O/P согласованы именованные и буквенные поля
ответа; противоречивые значения не выбираются произвольно. Если оба чтения
подтверждают только часть позиций или остаётся независимое свидетельство
непрочитанной строки, возвращается `partial_photo_read`; черновик, выбор
подразделения и финальная проверка сохраняют предупреждение о пропусках. Если
оба полных чтения подтверждают каждую заполненную строку и их количества
согласуются с доступным подсчётом, предупреждение не показывается. Спорные
товары, количества и комментарии в частичный результат не включаются.
Это не доказательство безошибочного OCR: одинаковая ошибка двух чтений возможна.

23.09 по логам фото добавлен отдельный контроль конфликта «пиксельный детектор
нашёл больше заполненных строк, чем вернул vision». Для таблиц с буквенными
колонками N/O/P выполняется дополнительное полное чтение до применения
колоночной сверки. Если оба завершённых чтения подтверждают все нормализованные
позиции и нет свидетельств пропущенной строки, результат считается полным даже
при расхождении служебной сверки сырых представлений. Если совпала только часть
позиций либо пиксельный счётчик независимо указывает на пропуск строки,
возвращается `partial_photo_read` с предупреждением; спорные строки исключаются.
При отсутствии подтверждённых строк бот по-прежнему не меняет черновик.

В рамках текущей архитектурной ветки сохранены внешние импорты и тестовые точки
подмены, но крупные модули разделены по владельцам. Structured vision-сверка
находится в `input/photo_observation_reconciliation.py`, prompt-контракты — в
`integrations/openai_text_prompt.py`, `openai_photo_prompts.py` и
`openai_decision_prompts.py` с тонким фасадом `openai_prompts.py`. Submission
разделён на `submission_access.py`, `submission_catalog.py`,
`submission_completion.py`, `submission_product_add.py`,
`submission_recalculation.py` и `submission_status.py`; основной координатор
теперь содержит 1919 строк. Фабрики БД, репозитория и chat-lock передаются в
вынесенные сервисы из владельца, поэтому production-путь и изолированные тесты
используют один и тот же dependency seam.

Второй проход декомпозиции вынес аналитические события в
`services/orchestrator_analytics.py`, модальный поток количества в
`services/engine_quantity_flow.py`, а подготовку снимка заявки и расчёт
распределения по подразделениям — в `services/engine_submission_preparation.py`.
После этого `orchestrator.py` содержит 1993 строки, `engine.py` — 1609 строк;
это оставшиеся координаторы переходов и контрольных точек, а не набор
несвязанных предметных правил.

Экран выбора подразделения показывает все активные позиции по 10 на странице.
В смешанном черновике кнопка отдела назначает его только неразмеченным товарам;
существующие распределения сохраняются. «Оставить как на фото» не подтверждает
черновик с неразмеченными товарами, в том числе через старый callback. Явный выбор
пользователя хранится отдельно от «Кухни» по умолчанию в `CartItem.department_confirmed`.
Новая фотография сбрасывает подтверждение заявки и страницу списка. Подтверждение
дубликата суммирует известные распределения; неизвестный отдел требует выбора
для новой суммы. Новые поля сохраняются в JSON состояния и не требуют SQL-миграции.

Путь распознавания сохраняет исходный кадр с EXIF-ориентацией и передаёт модели
полный контекст вместе с отдельным фокусным видом найденной таблицы. Изображения
уменьшаются только при превышении лимитов vision API; увеличенные вспомогательные
фрагменты также ограничены этими лимитами. Геометрия помогает найти сетку и
заполненные строки, но сама по себе не назначает Зал/Бар/Кухню. Распределение по
N/O/P разрешено только при распознанных буквах столбцов; иначе отделы не
угадываются по позиции. Обычная таблица «товар — количество», рабочий стол,
распечатанный список и фото списка используют общий разбор без навязанной схемы
подразделений.

Для частично видимых таблиц без доказанных заголовков и для обычных списков
числа в строке товара нормализуются как количество заказа без подразделения.
После сопоставления товаров используется существующий финальный выбор Зал/Бар/
Кухня. Значения не получают отдел из координат, положения или технического
поля ответа модели. Это же правило применяется, если видны отдельные ячейки
таблицы, но нельзя подтвердить, как они соответствуют подразделениям.

Распознавание произвольной фотографии не может гарантировать безошибочное OCR.
Неуверенные данные должны остаться неподтверждёнными; итоговый черновик нужно
проверять перед записью.

23.09 исправлен отдельный маршрут голосовых и текстовых команд. Фразы
«Добавить и проверить» и «Добавить в корзину и проверить» теперь сразу ведут в
финальную проверку и не попадают в каталог как товар «и проверить».
`parsing/commands/department.py` распознаёт короткий выбор подразделения
(`«Зал»`, `«на зал»`, `«добавить на зал»`, `«подразделение зал»` и аналогичные
формы для Бара и Кухни), передаёт его в существующий
`FinalReviewHandler` и не разрешает ИИ переписать его в товарную команду.
Тот же модуль теперь отделяет подразделение, указанное внутри строки товара:
`«Горчица 10 шт на зал»`, `«... в отдел кухни»`,
`«... подразделение бара»` и короткую форму `«... 10 шт зал»`. Такая строка
назначает отдел конкретной позиции до AI-разбора и не превращает маркер отдела
в комментарий или часть названия. Список из нескольких позиций разбирается
независимо по запятым, точкам с запятой и переносам: отдел каждой позиции не
переносится на соседнюю. Поддерживаются также слитные формы `10шт`,
`10штназал`, `накухнюХлеб2шт` и смешанный порядок «товар → отдел» /
«отдел → товар». Неоднозначное `«для кухни»` без явного указания отдела
сохраняется в названии товара, чтобы не повредить реальные позиции вроде
«Вино белое для кухни».

23.09 по локальным структурированным логам уточнён разбор списка без
разделителей: в обновлениях `247310994` и `247310998` отдел завершал первую
позицию, но следующая позиция склеивалась с ней или терялась при AI-разборе.
Владелец `parsing/commands/department.py` теперь признаёт границу после явно
указанного отдела, если ниже в той же фразе есть ещё один отдел. Названия с
запятыми и фасовками сохраняются; две позиции без количества остаются двумя
позициями с запросом уточнения, а не получают число из фасовки. Текст и
расшифрованный голос проходят общий детерминированный путь. В том же окне
ошибок транспорта и OpenAI не было; `247310989` уже разбирался на две позиции,
но для одной каталог ответил `product_not_found`, а `247311008` показал
`unit_mismatch` при выборе кандидата. Эти исходы нельзя автоматически
приписывать этому дефекту разбора или обходить небезопасной подстановкой.

23.09 обновление `247311011` выявило другой, уже не лексический дефект. ИИ
предложил две позиции с 19 кг для Зала и 5 кг для Бара; исходные локальные
фрагменты подтверждали оба отдела, но поле `source_department` осталось пустым.
Поэтому черновик считал их неподтверждёнными. При выборе кандидатов в обновлениях
`247311012`–`247311014` пользователь сам выбрал товары и изменил количество;
это не было автоматической подстановкой. Состояние затем сбросило даже явно
подтверждённый отдел при смене количества, потому что прежнее правило сохраняло
только единый отдел всего заказа. `parsing/ai/reconciliation.py` теперь сверяет
предложенный отдел с явным маркером в локальном фрагменте исходной фразы, а
`ConversationState.refresh_department_after_quantity_change` сохраняет
подтверждённый отдел конкретной позиции при смешанной заявке. Для фото с
распределением количества по колонкам прежнее требование повторного подтверждения
остаётся. Предложение ИИ без такого доказательства не подтверждается.

23.09 по отчёту тестировщика исправлена граница между фасовкой и количеством
при явном отделе: фраза `«Биттер Люксардо 0,75 л6бар»` теперь разбирается как
название с фасовкой `0,75 л`, заказ 6 и отдел «Бар». Количества соседних товаров
сохраняются независимо. Такой ввод обрабатывается детерминированно до AI, поэтому
воспроизведённый сценарий больше не отправляет отвергнутые числа в комментарий.
Соседние формы с пробелами, слипшейся единицей, каталожными фасовками и обычным
текстом/голосом покрыты регрессиями. Другие пункты отчёта — поиск частичного
названия в каталоге, расписание поставщика, преждевременные значения в таблице и
конкретное фото — требуют отдельного воспроизведения. Для поиска позиции в уже
собранном черновике частичный запрос, совпавший с несколькими товарами, теперь
показывает до пяти названий и просит уточнить вместо ложного «не найдено»; до
ответа черновик не меняется. Исходные фото и привязанные к ним логи в документ
не вложены, а расписание поставщика пока не имеет определённого формата и правила
подтверждения даты.

Сохранены также изменения GitHub-ветки по связанным защитам:

- `catalog/safety.py`: канонический безопасный слой эквивалентности теперь
  сравнивает словесные числа отдельно, сокращения упаковки, одну ограниченную
  ASR-ошибку слова и точное склеивание соседних токенов каталога. Выбор остаётся
  возможен только при совместимости числовых характеристик и ровно одном
  эквивалентном товаре.
- `catalog/evidence.py`: словесные числа не считаются неизвестным товарным
  признаком.
- Добавлены регрессии уровня каталога и полного AI-pending маршрута: точная
  голосовая форма выбирается до ИИ; другой вариант (`aka miso`) не подставляется.
- `conversation/routing/order_flow.py` и `services/engine.py`: `/reset` и
  «новый заказ» освобождают чат из `SUBMISSION_FAILED`, сохраняя старый
  `SubmissionRecord` для аудита и проверки.
- `services/submission.py`, `workers/tasks.py`, `workers/celery_app.py`:
  каждые пять минут retryable-снимки безопасно продолжаются, а начатые
  внешние отправки сверяются с листом «История» без повторного POST. Неопределённый
  перерасчёт повторяется по локальной контрольной точке до успеха; после
  успешного восстановления старая локальная заявка завершается, не изменяя новый
  черновик после `/reset`.
- `domain/models.py`, `services/engine.py` и `conversation_handlers/pending_quantity.py`:
  ручное или предложенное пользователю количество помечается как актуальное, поэтому
  запись заявки не подставляет устаревшее распределение по отделам.
- `presentation/telegram/submission.py`: сообщение о сбое перерасчёта объясняет
  автоматическую проверку и команду `/reset`.
- `presentation/telegram/replies.py` и `services/engine.py`: после `/reset` или
  «новый заказ» из неопределённой отправки бот предупреждает не оформлять старую
  заявку повторно; новый черновик при этом открывается сразу.
- `input/photo_views.py`: светлая тема Sheets теперь распознаёт и чёрные границы order/comment
  области, как на реальном пользовательском скриншоте. Геометрия остаётся дополнительным
  источником проверки и не требуется для обычных фото.
- `input/photo_ingestion.py`: same-row quantity разрешает order/free-list даже без заголовков,
  сетки и номеров строк. Self-reported `visible_filled_order_row_count` модели больше сам по
  себе не отклоняет фото. Фраза «все товары ...» и эквивалентные явные маркеры поднимаются
  в общий комментарий и не дублируются на одной позиции.
- `integrations/openai_client.py` и `integrations/openai_prompts.py`: повторный проход работает
  также для free-list/headerless фото; vision явно получает контракт, что таблица, колонки и
  номера строк необязательны, а комментарии нужно сохранять и для позиции, и для всей заявки.
- `domain/departments.py`: технический отдел `condiments` нормализуется в «Кухня».
- `integrations/google_sheets.py` и `services/submission.py`: запись fail-closed при дубликате
  product_id, изменившейся идентичности товара, неизвестной колонке отдела и пустом quantity-плане.
  Пустой план больше не может завершиться сообщением об успешной записи.
- Кнопки выбора кратности теперь несут ID конкретной позиции, поэтому выбор количества не может
  примениться к соседнему похожему товару после изменения текущего modal-контекста.

Если видимый номер строки фотографии не совпадает с кэшированным номером строки
каталога, он не создаёт пустую карточку «несколько вариантов». Такой номер не
достаточен для выбора товара: обработка продолжает безопасное сопоставление по
прочитанному названию. Карточка кандидатов выводится только при реальном списке
кандидатов; непредвиденное пустое состояние использует обычное восстановление
«товар не найден».

## Изменённые владельцы

- `services/submission.py` больше не владеет всеми побочными протоколами: доступ,
  статусы, запрос снабженцу, перерасчёт и уведомление о завершении делегируются
  тематическим сервисам. Аналитические события, поток количества и подготовка
  снимка заявки вынесены из `services/orchestrator.py` и `services/engine.py`;
  тонкие методы-адаптеры сохранили прежние внутренние точки вызова и тестовые
  подмены.
- `integrations/openai_prompts.py` стал совместимым фасадом над тематическими
  prompt-модулями; шесть prompt-констант проверены на побайтное совпадение с
  исходной версией.

- `input/photo_views.py`: EXIF, полнокадровый и фокусный виды, детектор сетки и
  контроль бюджета пикселей/patches для исходных и производных кадров.
- `parsing/commands/department.py`, `parsing/commands/patterns.py` и
  `input/telegram_interpretation.py`: детерминированные маршруты голосовой
  команды финальной проверки и выбора подразделения до каталога и AI-разбора;
  отдел внутри товарной строки выделяется до AI-разбора и записывается в
  `ExtractedItem.department`; отдельное количество после фасовки распознаётся
  только рядом с явным отделом, не теряя часть названия товара.
- `integrations/openai_client.py`: один согласованный structured vision-запрос
  на проход, стабильные параметры модели и проверка столбцов только при явных
  заголовках/буквах.
- `input/photo_ingestion.py`: классификация и нормализация без вывода отдела
  из геометрического положения; неразмеченные ячейки превращаются в обычное
  количество и идут в штатный финальный выбор подразделения.
- `orders/catalog_resolution.py`: номер строки фото не блокирует поиск по
  названию, если в каталоге для него нет единственной подтверждённой строки;
  автоматический выбор требует подтверждения всех существенных слов, поэтому
  «дижонская» не объединяется с «зернистой» только из-за общих слов.
- `presentation/telegram/replies.py`: карточка выбора требует непустой список
  вариантов и не показывает пользователю ложный пустой выбор. На экране выбора
  подразделения для одной позиции используется форма «Позиция 1 из 1» с
  визуальными отступами вокруг списка. Черновик, выбор подразделения и
  финальная проверка показывают не более 10 позиций на странице; история
  заявки использует тот же предел для товарных строк.
- `services/engine.py`: добавление комментария разрешается только при наличии
  активной позиции в черновике или одновременно добавляемого товара. Пустой
  черновик больше не получает ложное уведомление «Комментарий добавлен» и не
  создаёт отложенную область комментария без товара.
- `application/order_review/contracts.py`, `application/order_review/snapshot.py`
  и `presentation/telegram/order_review.py`: проверка заявки по ссылке из таблицы
  сохраняет распределение количества по Залу, Бару и Кухне, показывает его рядом
  с каждой позицией и включает это распределение в отпечаток снимка. При отправке
  через центральный Web App источником данных остаётся исходная таблица заведения;
  бот передаёт её `sourceSpreadsheetId`, а колонки подразделений читаются из неё.
- `observability/tracing.py`: Langfuse получает ограниченные параметры фоновой
  отправки, корневая трасса явно сбрасывается после запроса, а сбой flush
  логируется и не ломает обработку Telegram.
- `pyproject.toml` и `uv.lock`: Langfuse зафиксирован на `4.14.4`, последней
  версии, которая подтверждённо создавала трассы в текущем проекте; сборка
  больше не подтягивает произвольную версию из диапазона.
- Регрессии проверяют изображения разных размеров, десктопный контекст, EXIF,
  сжатые таблицы, неразмеченные ячейки с финальным выбором подразделения,
  запрет угадывания подразделения, отсутствие пустой карточки кандидатов и
  различение вариантов товара с одинаковыми общими словами.

## Проверки и следующий шаг

- Связанный набор регрессий отчёта: `162 passed` в разборах текста/отделов,
  `175 passed` в разборах текста/отделов, поиске и изменениях черновика. Он
  проверяет text/voice с фасовкой `0,75 л6бар`,
  обход AI для подтверждённого отдела, раздельные количества и комментарии,
  неоднозначный поиск при удалении/изменении количества без мутации состояния.
- Неподтверждённые пункты отчёта: если частичный поиск выполнялся в каталоге, нужны
  точный запрос, состояние диалога и экран товаров; для календаря — формат графика
  и правило выбора ближайшей даты; для значений в Sheets — время записи и номер
  заявки; для фото — оригинал снимка и логи обработки.
- Последний полный `pytest` после регрессий локального отдела и правки количества
  завершился с кодом 0 (`2033` теста); прямые тесты проверяют правильный отдел
  у двух позиций, отказ для переставленных ИИ отделов и «для кухни», а также
  сохранение разных отделов при выборе кандидата и исправлении единицы.
  Ruff format/check по `src tests scripts alembic`, `mypy src`, проверка 42
  UX-сценариев, Markdown-ссылок (38 файлов) и `git diff --check` прошли.
  Полный pytest требует доступа к собственному временному каталогу: ограниченная
  Windows-песочница давала `WinError 5`, повторный прогон вне неё прошёл.
  OpenAI и Telegram в автоматических тестах не вызывались реально.
- Проверены повторный ввод фото/текста/голоса, сохранение состояния в JSON,
  выбор отдела до/после первого подтверждения, старый callback, пагинация
  смешанного списка, объединение дубликатов, изменение количества, соседние
  сценарии текста/голоса и маппинг количества для отправки.
- В текстовом обновлении из логов товары были извлечены, но каталог оставил
  неоднозначный вариант для выбора пользователя. Это этап безопасного уточнения,
  а не сбой распознавания текста; автоматический выбор без единственного
  подтверждённого совпадения не включался.
- Vision в автоматических тестах подменён контролируемыми ответами; реальные
  изображения через OpenAI в этой задаче не отправлялись. Для N/O/P добавлена
  регрессия на конфликт счётчика строк: две согласованные контрольные расшифровки
  сохраняют подтверждённые строки как частичный результат с предупреждением;
  несогласованные позиции остаются вне черновика. Гарантии безошибочного чтения
  любого физически нечитаемого изображения нет.
- 23.09 локальные `api`, `worker` и `beat` пересобраны из текущей рабочей копии
  и пересозданы без перезапуска PostgreSQL и Redis. `api` и `worker` здоровы,
  `/health/live` и `/health/ready` отвечают `200`. Коммит, push, серверное
  развёртывание, туннель и вебхук не менялись. Внешняя проверка исправленного
  текстового сценария и последовательных фотографий остаётся ручным шагом;
  автоматические тесты использовали контролируемые ответы интеграций.
- 23.09 по логам фото с отделами 6 «Кухня», 20 «Зал», 2 «Бар» подтверждён сброс
  назначений после принятия допустимых количеств; дополнительно подготовка заявки
  сводила отредактированные строки к одному отделу. Теперь обновлённое количество
  сохраняется в исходном отделе, а подготовка записи сохраняет только распределение,
  сумма которого совпадает с количеством строки. Проверено последовательное
  принятие 21 «Зал» и 3 «Бар» без повторного выбора общего отдела; смешанное
  распределение при изменении суммы всё ещё требует уточнения.
- Сброс чата при фоновой проверке больше не утверждает, что поставщики могли получить
  заявку до начала отправки. После успешного восстановления локальной записи бот
  отправляет отдельное подтверждение без кнопок, меняющих текущий черновик; состояние
  уведомления фиксируется, а неопределённую доставку бот не повторяет автоматически.
  Целевые проверки количества, маршрутизации `/reset` и отправки прошли. Полные
  Ruff/mypy/pytest и проверка опубликованного поведения остаются перед выпуском.
- 23.09 на update `247311129` фото с тремя заполненными строками прошло два полных
  vision-чтения; обе нормализации дали по три строки, независимый пиксельный счётчик
  отсутствовал, но строгая сверка буквенных колонок отклонила второе чтение и
  безусловно поставила `partial_photo_read`. Теперь одинаковый полный набор строк
  без свидетельств пропуска считается полным; частичное предупреждение сохраняется
  при реальном расхождении или подтверждённом счётчиком пропуске. Финальная проверка
  также не повторяет количество, когда вся позиция относится к одному отделу.
  Регрессии на полное совпадение чтений, реальный конфликт пиксельного счётчика,
  неполное фото и распределение по нескольким отделам добавлены; полный pytest,
  Ruff, mypy, проверки сценариев, ссылок и diff прошли. Локальные `api`, `worker`
  и `beat` пересобраны; миграция завершилась с кодом 0, `/health/live` и
  `/health/ready` отвечают. Сервер и туннель не затрагивались.
