# Передача контекста проекта

## Текущее состояние

По анализу `vmui_logs_export.jsonl` исправлены ошибки разбора входных сообщений в
общих владельцах правил, без изменения системных промптов и инфраструктуры.

Исправлены:

- граница семантического списка: склонения товара больше не приводят к потере
  подтверждённых позиций;
- словесные количества и формы единицы «мешок» восстанавливаются из исходной
  фразы, при этом не создаётся неуказанное количество;
- разговорные и ошибочно распознанные вводные слова не попадают в
  `product_query`;
- приветствия и свободная беседа отделены от истории поставок;
- близкий кандидат каталога сохраняется для выбора, если противоречие добавлено
  моделью, а не подтверждено требованиями пользователя;
- добавлены регрессионные проверки для списков, количества, приветствий,
  разговорных вводных и кандидатов.

## Изменённые владельцы

- `parsing/semantic/boundaries.py` — сопоставление якорей с безопасным учётом
  окончаний слов;
- `parsing/ai/quantity_reconciliation.py` и `domain/units.py` — происхождение
  количества и нормализация единиц;
- `parsing/ai/comment_reconciliation.py`, `parsing/products.py` и
  `parsing/commands/item_commands.py` — очистка разговорных оболочек;
- `parsing/semantic_routing.py` и `input/telegram_interpretation.py` — граница
  приветствий, беседы и неподтверждённой истории;
- `integrations/openai_client.py` — безопасный резерв после сетевого таймаута;
- `catalog/retrieval.py`, `catalog/resolver.py` и `orders/catalog_resolution.py` —
  похожие кандидаты только для ручного уточнения;
- `services/orchestrator.py` — безопасное сохранение близкого кандидата;
- соответствующие тесты в `tests/`.

## Проверки

- полный локальный `pytest`: **успешно**;
- связанные сценарии истории, голоса, каталога, оркестратора и Telegram:
  **успешно**;
- `ruff check`, `ruff format --check`, `git diff --check`: **успешно**.

## Ограничения и риски

В логах отдельно видны задержки/повторы OpenAI, ошибки Telegram-соединения,
обновление кэша каталога и неопределённый результат внешней отправки. Это
операционные проблемы и не смешивались с исправлением парсинга; для них нужна
отдельная проверка окружения и метрик.

Реальная голосовая/фотографическая проверка и поведение удалённого окружения
этим набором локальных тестов не подтверждены.

## Локальный запуск

- API/worker/beat пересобраны из текущего рабочего дерева; миграции завершились
  успешно.
- Проверки `health/live` и `health/ready` локально и через туннель возвращают
  `200`.
- Запущен Quick Tunnel:
  `https://guides-wide-matched-grant.trycloudflare.com`.
- Telegram webhook установлен на
  `https://guides-wide-matched-grant.trycloudflare.com/webhooks/telegram`;
  `pending_update_count=0`, ошибок Telegram нет.
- `GOOGLE_ORDER_SUBMISSION_ENABLED=false`; внешняя отправка заявок отключена.

## Последняя проверка логов

- Обновление `247310152` завершилось технической ошибкой: OpenAI `APITimeoutError`. При таймауте 30 секунд SDK сделал повтор, поэтому пользователь ждал около 61 секунды; детерминированный разбор (`add_items`, одна позиция) не был применён как резервный результат.
- В обновлениях `247310178` и `247310183` `text_ai_parsed` содержал явные количества, но `text_command_normalized` их изменил или удалил. Это остаточный дефект границы `quantity_reconciliation`, а не Telegram.
- Для опечатки `яблак` (`247310150`) каталог вернул `candidate_count=0`; похожий вариант не предложен. Это отдельный пробел поиска/подсказок каталога.
- Голосовые изменения количества (`247310176`, `247310177`, `247310181`) и callback-действия отработали успешно. В проверенном диапазоне разговорные сообщения в `history_query` не ушли.
- Контейнеры здоровы, планировщик redrive запускается каждую минуту, но после сбоя `247310152` повторно поставленных обновлений не обнаружено (`update_count=0`).

## Исправлено в текущей итерации

- При сетевом таймауте OpenAI для одной однозначной позиции применяется только
  подтверждённый детерминированный разбор. Списки, комментарии, диапазоны и
  принудительный AI-режим по-прежнему требуют ответа модели и не угадываются.
- Границы позиций учитывают словесное количество перед товаром, поэтому
  количества в фразах вроде «один килограмм ... и два килограмма ...» не
  перераспределяются между товарами.
- Для опечаток добавляются отдельные похожие кандидаты с причиной `similar`;
  они не выбираются автоматически и не удаляются из списка после ответа модели
  `not_found`. Числовые характеристики и ограничение поставщика проверяются.
- Если ИИ вернул `unknown`, указательная фраза без самостоятельного названия
  товара не восстанавливается детерминированно в новую позицию. Это устраняет
  расхождение, когда пользователю показывали «не распознано», а метрика всё же
  записывала успешное `add_items`.
- Новый названный товар без количества теперь безопасно прерывает открытую
  карточку `not_found`, сохраняет старую проблемную строку и проходит обычную
  сверку каталога. Ранее такой ввод ошибочно считался ответом на старую карточку.
- Если ИИ пропустил локальное требование, начинающееся с явного маркера
  («обязательно», «желательно», «только» и т. п.), оно восстанавливается только
  из `source_span` своей позиции и сохраняется комментарием. Привязки ИИ и
  глобальные комментарии этим резервным путём не изменяются.
- Роль весового диапазона подтверждается исходной фразой, а не уверенным
  значением модели: без явной просьбы о фасовке диапазон остаётся каталожным
  признаком, а отдельное «нужно 10 кг» — количеством заказа.
- Разговорные формы «картошка/картошки/картошку» дают только похожие варианты
  каталожного «картофеля». Они не считаются точным совпадением и не выбираются
  автоматически.
- Словесное «пару <товар>» определяется по локальному фрагменту после этого
  маркера. Единица соседней позиции, например «две бутылки водки и пару
  селедки», не переносится на второй товар.
- Полный набор из 100% тестов прошёл с `PYTHONPATH=.`; `mypy src` также завершён
  без ошибок. Первый запуск полного pytest без `PYTHONPATH=.` дал только ошибки
  импорта пакета `scripts`, поэтому он не считается проверкой кода.

## Следующий шаг

Выполнить ручной сценарий в Telegram на чистом черновике (`/reset`): голосом
«филе форели 0,8–1,3 кг, обязательно зачищенные ..., нужно 10 кг, и картошка
5 кг». У форели должны остаться диапазон и количество 10 кг, а пожелание —
в комментарии; по картошке должны быть только варианты картофеля без
автоподстановки. Также проверить `яблак 15 кг`, «Картофель старый» при открытой
карточке ненайденного товара и фразу «Все эти лежитки оранжевые». Quick Tunnel
работает только пока запущен процесс `cloudflared`; при его смене нужно обновить
`PUBLIC_BASE_URL`, пересоздать сервисы и повторно выполнить `set-webhook`.
Коммит и отправка в `develop` в этой итерации не выполнялись.
